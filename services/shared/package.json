{
    "name": "@thawgate/shared",
    "version": "0.1.0",
    "description": "Shared utilities, Prisma client, and types for SSS backend services",
    "private": true,
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "test": "echo 'No tests implemented yet for shared'",
        "db:generate": "prisma generate",
        "db:migrate": "prisma migrate dev",
        "db:push": "prisma db push"
    },
    "dependencies": {
        "@prisma/client": "^5.10.0",
        "@solana/web3.js": "^1.95.0",
        "zod": "^3.22.0"
    },
    "devDependencies": {
        "typescript": "^5.4.0",
        "@types/node": "^20.0.0",
        "prisma": "^5.10.0"
    }
}